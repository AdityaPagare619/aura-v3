# AURA v3 — Pinned Termux Requirements
# Only pip-installable CORE dependencies are listed here.
# Enhanced deps (cryptography, numpy) are installed via Termux pkg.
# Optional deps (llama-cpp-python) are handled separately.
#
# To install: pip install -r requirements-termux.txt

aiofiles>=24.1.0
PyYAML>=6.0.2
psutil>=6.1.0
python-telegram-bot>=21.0
